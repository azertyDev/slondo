import {makeStyles} from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
    root: {
        marginTop: '10px',
    },
    bottomHeaderWrapper: {
        marginTop: '60px'
    }
}))