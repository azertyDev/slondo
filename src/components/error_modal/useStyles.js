import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    root: {
        width: '600px',
        height: '400px',
        backgroundColor: '#fff',
    },
}));
