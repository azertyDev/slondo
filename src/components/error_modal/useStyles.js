import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    root: {
        width: '600px',
        height: '400px',
        margin: '40px auto',
        backgroundColor: '#fff',
    },
}));
