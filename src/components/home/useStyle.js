import {makeStyles} from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
    mainSlider: {
        marginTop: '20px'
    },
    categorySlider: {
        marginTop: '10px'
    },
    mainContent: {
        marginTop: '30px'
    }
}))