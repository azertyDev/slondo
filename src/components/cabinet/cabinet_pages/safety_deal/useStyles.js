import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
        '& div.wrapper': {
            background: '#F2F2F2',
            borderRadius: '10px',
            width: '955px',
            height: '793px',
        },
    },
}));
